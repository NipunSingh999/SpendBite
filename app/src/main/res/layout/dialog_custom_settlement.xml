<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/card_bg"
    android:layout_margin="16dp">

    <!-- Header Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:layout_marginBottom="12dp">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_profile"
            app:tint="@color/brand_primary" />

        <TextView
            android:id="@+id/tv_settle_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:text="Settle Debt"
            android:textColor="@color/brand_primary"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Message Body -->
    <TextView
        android:id="@+id/tv_settle_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose an action:"
        android:textColor="@color/text_zinc_500"
        android:textSize="14sp"
        android:layout_marginBottom="24dp" />

    <!-- Action Buttons Column -->
    <Button
        android:id="@+id/btn_settle_remind"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/button_accent_bg"
        android:text="Send Payment Reminder"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        app:backgroundTint="@null"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_settle_mark"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/button_outline_bg"
        android:text="Mark as Settled Offline"
        android:textAllCaps="false"
        android:textColor="@color/brand_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        app:backgroundTint="@null"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_settle_cancel"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackground"
        android:text="Cancel"
        android:textAllCaps="false"
        android:textColor="@color/text_zinc_500"
        android:textSize="14sp"
        android:textStyle="bold" />
</LinearLayout>
